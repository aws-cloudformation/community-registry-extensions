Resources:
  MyBucket: 
    Type: AwsCommunity::S3::Bucket::MODULE
    Properties:
      BucketName: !Sub "cep-integ-s3-bucket-module-${AWS::AccountId}"
      LogBucketName: !Sub "cep-integ-log-s3-bucket-module-${AWS::AccountId}"
    
